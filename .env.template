# =====================================
# DATABASE CONFIGURATION
# =====================================
DATABASE_URL="file:./dev.db"

# =====================================
# NEXTAUTH CONFIGURATION
# =====================================
# Generate NEXTAUTH_SECRET using:
# - Linux/Mac: openssl rand -base64 32
# - Windows: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-generate-a-secure-random-string"

# =====================================
# BUNNY STREAM CONFIGURATION
# =====================================
# Obtain these from your Bunny CDN dashboard
# https://bunny.net/dashboard/
BUNNY_STREAM_API_KEY="your-bunny-api-key-here"
BUNNY_STREAM_LIBRARY_ID="your-library-id-here"
BUNNY_STREAM_CDN_HOSTNAME="your-cdn-hostname.b-cdn.net"

# =====================================
# OPTIONAL: PUBLIC URL (for production)
# =====================================
# NEXT_PUBLIC_APP_URL="https://yourdomain.com"

# =====================================
# SECURITY NOTES
# =====================================
# 1. NEVER commit this file to version control
# 2. Use strong random strings for secrets (minimum 32 characters)
# 3. In production, use your hosting platform's secret management
# 4. Rotate secrets regularly (every 90 days recommended)
# 5. Limit access to this file (use proper file permissions)
